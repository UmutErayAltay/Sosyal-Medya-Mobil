# İmzalama anahtarı + şifresi — ASLA commit edilmez (uygulamayı güncellemenin tek yolu bu dosya + şifresi, sızarsa/kaybolursa geri dönüşü yok)
android.keystore
*.jks
*.keystore
KEYSTORE_INFO.txt

# Gradle / Android build çıktıları
.gradle/
build/
local.properties
*.apk
*.aab

# 2026-08-08: kullanıcı isteğiyle native app'in indirilebilir debug APK'sı
# releases/ altında BİLEREK commit edilir (elle güncellenir, otomatik
# derleme/CI YOK — güncel tutmak isteyen bunu her defasında elle üretip
# değiştirmeli).
!releases/*.apk

*.idsig
*.log
manifest-checksum.txt

# IDE
.idea/
*.iml
.DS_Store

# Mimari & aktif görev dokümanları (sosyal-medya deposundaki .context/ ile
# AYNI model — kod taramadan önce buraya bakılır, ama repoya commit edilmez,
# sadece içeriği serbestçe güncellenir)
.context/
